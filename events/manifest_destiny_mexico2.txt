manifest_destiny_mexico1 = { #UNO Reverse

	is_shown = {
		exists = c:MEX
		root = c:MEX
		NOT = { has_variable = manifested_destiny_pov_mexico1 }
	}

	possible = {
		has_technology_researched = nationalism
		has_interest_marker_in_region = region_central_america
	}

	when_taken = {
		if = {
			limit = {
				owns_entire_state_region = STATE_HONDURAS
				owns_entire_state_region = STATE_NICARAGUA
				owns_entire_state_region = STATE_COSTA_RICA
				owns_entire_state_region = STATE_SAN_SALVADOR
				owns_entire_state_region = STATE_YUCATAN
				owns_entire_state_region = STATE_CHIAPAS
				owns_entire_state_region = STATE_GUATEMALA
			}
			trigger_event = { id = manifest_destiny_mexico.2 }
		}
		else = {
			add_journal_entry = {
				type = je_manifest_destiny_pov_mexico2
			}
		}
		# Add Claims
		s:STATE_HONDURAS = {
			if = {
				limit = {
					any_scope_state = {
						 NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_NICARAGUA = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_COSTA_RICA = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_SAN_SALVADOR = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_YUCATAN = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_CHIAPAS = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_GUATEMALA = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		set_variable = manifested_destiny_pov_mexico1
	}

	ai_chance = {
		value = 10
	}
}
