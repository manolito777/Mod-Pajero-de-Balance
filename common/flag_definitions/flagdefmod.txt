REPLACE_OR_CREATE:SAR = { # Sardignia-Piedmont
	flag_definition = {
		coa = SAR_austria
		subject_canton = SAR_canton
		coa_with_overlord_canton = SAR_subject
		overlord_canton_offset = { 0.067 0.1 }
		overlord_canton_scale = { 0.4 0.4 }
		allow_overlord_canton = yes
		priority = 1
		trigger = {
		exists = scope:actor
			scope:actor ?= {
				has_variable = sardiniaaustriavar
			} 
		}
	}
}
REPLACE_OR_CREATE:ITA = { # ITALIA AUSTRIA
	flag_definition = {
		coa = ITA_AUS
		subject_canton = ITA_AUS
		priority = 1
		trigger = {
		exists = scope:actor
			scope:actor ?= {
				has_variable = sardiniaaustriavar
			} 
		}
	}
}
REPLACE_OR_CREATE:GER = {
	flag_definition = {
		coa = GERCOAMOD
		subject_canton = GERCOAMOD
		priority = 1
		trigger = {
		exists = scope:actor
			scope:actor ?= {
					has_law = law_type:law_Kaiserliche_Monarchie
			} 
		}
	}
}
REPLACE_OR_CREATE:GBR = { # BRITANNIA
	flag_definition = {
		coa = GBR_BRI
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
				has_variable = japanukbloc
			} 
		}
	}
}
INJECT:JAP = { # BRITANNIA
	flag_definition = {
		coa = JAP_11
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
				has_variable = japanukbloc
			} 
		}
	}
}
REPLACE_OR_CREATE:MEX = {
	flag_definition = {
		coa = AR1
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:CLM = {
	flag_definition = {
		coa = AR2
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:GCO = {
	flag_definition = {
		coa = AR2
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:BOL = {
	flag_definition = {
		coa = AR3
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:PBC = {
	flag_definition = {
		coa = AR3
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:FND = {
	flag_definition = {
		coa = AR3
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:ARG = {
	flag_definition = {
		coa = AR4
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:PLT = {
	flag_definition = {
		coa = AR4
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:BRZ = {
	flag_definition = {
		coa = SHBE
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:DAI = {
	flag_definition = {
		coa = AR10
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
REPLACE_OR_CREATE:INC = {
	flag_definition = {
		coa = AR10
		subject_canton = GBR_BRI
		priority = 50
		trigger = {
		exists = scope:actor
			scope:actor ?= {
			is_in_same_power_bloc = c:GBR
			}
			c:JAP = { 
	        has_variable = japanukbloc 
	    
	        } 
		}
	}
}
