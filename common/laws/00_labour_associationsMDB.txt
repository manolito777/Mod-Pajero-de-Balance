
######MOD########
CREATE_OR_REPLACE:law_artisan_system = {
	group = lawgroup_labour_associations
    parent = law_guild_system # Inherits ideological stances from parent
	icon = "gfx/interface/icons/law_icons/artisanal_economy.dds"

	progressiveness = -50
	
	is_visible = {
		OR = {
			c:USA ?= this
			AND = {
				exists = c:USA
				is_subject_of = c:USA
			}
		}
	}

	on_enact = {
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_right_to_associate
					has_law_or_variant = law_type:law_corporatized_unions
				}
			}
			custom_tooltip = trade_unions_more_radical_effect_tt
		}
		if = {
			limit = {
					has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
	}

	disallowing_laws = {
		law_laissez_faire
	}

	modifier = {
		# Limits building to self-investing

		building_group_bg_manufacturing_allowed_collectivization_add = 0.25
		building_group_bg_mining_allowed_collectivization_add = 0.25
		building_group_bg_extraction_allowed_collectivization_add = 0.25
		building_group_bg_power_allowed_collectivization_add = 0.25
		building_group_bg_private_infrastructure_allowed_collectivization_add = 0.25
		building_group_bg_arts_allowed_collectivization_add = 0.25

		country_shopkeepers_pol_str_mult = 0.35

		state_pop_qualifications_mult = -0.10
	}

	ai_will_do = {
		NOT = { has_technology_researched = corporatism }
		any_interest_group = {
			is_in_government = yes
			leader ?= {
				OR = {
					has_ideology = ideology:ideology_traditionalist
					has_ideology = ideology:ideology_traditionalist_minoritarian
					has_ideology = ideology:ideology_legitimist
				}
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}
CREATE_OR_REPLACE:Norddeutsche_Gewerbeordnung = {
	group = lawgroup_labour_associations
    parent = law_combination_acts # Inherits ideological stances from parent
	icon = "gfx/interface/icons/law_icons/guildpru.dds"

	progressiveness = -50
	
	is_visible = {
		OR = {
			c:PRU ?= this
			c:GER ?= this
		}
	}

	on_enact = {
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_right_to_associate
					has_law_or_variant = law_type:law_corporatized_unions
				}
			}
			custom_tooltip = trade_unions_more_radical_effect_tt
		}
		if = {
			limit = {
					has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
	}

	disallowing_laws = {
		law_laissez_faire
	}

	modifier = {
		# Limits building to self-investing


		interest_group_ig_industrialists_pol_str_mult = 0.10
        state_pop_support_movement_labor_mult = 0.1
		state_radicals_from_political_movements_mult = -0.10
	}

	ai_will_do = {
		NOT = { has_technology_researched = corporatism }
		any_interest_group = {
			is_in_government = yes
			leader ?= {
				OR = {
					has_ideology = ideology:ideology_traditionalist
					has_ideology = ideology:ideology_traditionalist_minoritarian
					has_ideology = ideology:ideology_legitimist
				}
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}
