amendment_brasil_slaves = {

	parent = law_slave_trade 

	allowed_laws = {
		law_slave_trade 
	}

	modifier = {
		interest_group_ig_landowners_approval_add = 5
		interest_group_ig_landowners_pol_str_mult = 0.1
		state_slave_import_mult = 0.5
	}

	possible = {
		OR = {
			country_has_primary_culture = cu:brazilian
		}
	}

	would_sponsor = {
		AND = {
			exists = scope:approval
			scope:approval > 0
		}
	}
}